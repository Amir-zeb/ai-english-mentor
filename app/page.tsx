"use client";

import Chat from "@/components/chat/chat";

export default function Home() {
// later home page will be implemented with a fetch api and pass conversation history and title of the mentor used, for now it will just render the chat component directly

  return (
    <Chat />
  );

}
